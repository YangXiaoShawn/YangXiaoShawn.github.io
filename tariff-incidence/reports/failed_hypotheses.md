# Failed, Unstable and Unresolved

> **DATA PROVENANCE: OFFICIAL SOURCES**
>
> All figures below derive from official statistical or legal sources.
>
> run_id `20260811T201708Z-b1495fb3` · git `3c51a06-dirty` · config `sample_slice.yaml` (sha256 `b1495fb3b363`) · data period 2017-01 to 2020-02 · generated 2026-08-11T20:17:08.149704+00:00


Recording what did not work is part of the result. Everything here was found by the pipeline and is reported whether or not it is convenient.

## Findings that did not hold up, and work that did not succeed

- **pretrend_stacked_log_customs_unit_value_never_treated_products_treated_country_only**: pre-trend verdict `PRETREND_PRESENT` (max |pre coef| 0.0736, relative to mean post 1.1886244869939997). Post-treatment coefficients from this specification should not be read as clean dynamic effects.
- **placebo_treatment_date_minus_12m** returned a significant placebo effect (max |coef| 0.09339949026408781). This is evidence against the design, not a nuisance.
- **Ground-truth recovery is imperfect.** The generator injects a quantity elasticity of -1.5 with respect to log(1 + additional duty). The PPML estimate is attenuated toward zero. This most likely arises because the treated country is the largest supplier in the sample, so a common month fixed effect partially absorbs the variation it is meant to difference out. This is a real feature of Section 301 settings, where China is a dominant supplier, and it argues against saturating with product-time effects when the level response is the estimand.
- **List 4A was not parsed, and now is.** Its annex enumerates 10-digit statistical lines under a different construction from Lists 1-3, and the original parser raised rather than guessing at it. That was the right failure: the parser was extended to the second construction instead of the codes being inferred. The schedule now carries SEC301_LIST1, SEC301_LIST2, SEC301_LIST3, SEC301_LIST4A, and the window extends past 2019-08.
- **Product exclusions cannot be incorporated, and this is now established rather than assumed.** Across 11 notices, only 1.9% of exclusions (16 of 840) are expressed as a 10-digit subheading; the rest describe a subset of a statistical reporting number, which published trade data cannot resolve. Every annex is a raster image with no text layer. The milestone is closed as **not achievable from published statistics**, not as outstanding work.
- **"The Census concordance is unavailable" was wrong, twice over.** The reference URLs returned 404/403 and the conclusion drawn was that the resource did not exist; it did, and the guessed URL patterns were the failure. Then the per-year files were declared unreadable because openpyxl rejects the legacy `.xls` vintages — but `xlrd` reads exactly those and nothing else. One tool failing is not a resource being unavailable. Industry mapping now uses `CENSUS_IMPORT_CONCORDANCE_2017_PRIMARY_2017_2020_HS10_TO_NAICS_TO_BEA`, and the hand-built chapter map survives only as a labelled fallback.
- **One tariff line was lost to typesetting.** In 83 FR 28710 a code renders as `9033.00` with its final pair missing. It was not repaired by guessing; it was resolved only because the USITC HTS contains exactly one unclaimed 8-digit line under that heading, and the resulting record is marked DERIVED rather than OFFICIAL_PARSED.

---

## Reproducibility

- run id: `20260811T201708Z-b1495fb3`
- git commit: `3c51a06-dirty`
- configuration: `sample_slice.yaml` (sha256 `b1495fb3b363c158ae3b162babd17a515adfc9a8c26038b6c09edb9ec55652a3`)
- data provenance: `OFFICIAL`
- data period: 2017-01 to 2020-02
- generated: 2026-08-11T20:17:08.149704+00:00
- python 3.12.13 on macOS-26.6.1-arm64-arm-64bit

_This document is generated by `scripts/generate_reports.py`. Do not edit it by hand; edit the generator or the underlying result tables._
